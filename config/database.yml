# MySQL.  Versions 4.1 and 5.0 are recommended.

# 

# Install the MYSQL driver

#   gem install mysql

#

# Ensure the MySQL gem is defined in your Gemfile

#   gem 'mysql'

#

# And be sure to use new-style password hashing: ts_mst_aug_03 bima_ts_srs ticket_simply_test

#   http://dev.mysql.com/doc/refman/5.0/en/old-client.html

development:
  host: 127.0.0.1
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: sample_app
  pool: 5
  username: root
  password:
  #socket: /tmp/mysql.sock

# Warning: The database defined as "test" will be erased and

# re-generated from your development database when you run "rake".

# Do not set this db to the same as development or production.

test:
  host: 127.0.0.1
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: sample_app_test
  pool: 5
  username: root
  password:
 #socket: /tmp/mysql.sock


production:
  host: 127.0.0.1   
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: sample_app
  pool: 5
  username: root
  password:
  #socket: /tmp/mysql.sock